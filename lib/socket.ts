// import { Server as ServerIO } from "socket.io";

// let io: ServerIO | null = null;

// export const setIOInstance = (server: ServerIO) => {
//   io = server;
// };

// export const getIOInstance = (): ServerIO => {
//   if (!io) {
//     throw new Error("Socket.io instance not initialized");
//   }
//   return io;
// };
